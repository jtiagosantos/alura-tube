import { styled } from '../styles/theme';

export const Banner = styled('div', {
  w: '100%',
  h: '230px',
  background:
    "linear-gradient(to bottom, rgba(0,0,0,0), rgba(0,0,0, 0.85)), url('/assets/nintendo-control.jpg')",
  backgroundPositionY: 'center',
});
